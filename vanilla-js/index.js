// Import the counter component
import '../components/counter/counter.js';

// The component is registered as a custom element in the imported file,
// so we don't need to do anything else here.
console.log('Vanilla JS implementation loaded');